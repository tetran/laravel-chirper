## How to start

1. Comfirm PHP (^8.2) is installed. If not, see [Installing PHP and the Laravel Installer](https://laravel.com/docs/12.x#installing-php).

2. Run `composer install`.

3. Run `php artisan key:generate && php artisan migrate`.

4. Run `composer run dev`.
